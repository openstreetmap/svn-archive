dojo.provide("cmyk.rules._Symbol");

dojo.require("cmyk.rules.renderingInstruction");

/**
	@lends cmyk.rules._Symbol
*/

dojo.declare("cmyk.rules._Symbol",cmyk.rules.renderingInstruction,{
	/** 
	      @constructs
	      @class This is the superclass of every symbol rendering instruction
	      @memberOf cmyk.rules
	      @extends cmyk.rules.renderingInstruction
	      @private
	*/
	constructor: function() {
	},
});

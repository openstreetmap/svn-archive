dojo.provide("cmyk.rules._wayAreaText");

dojo.require("cmyk.rules.renderingInstruction");

/**
	@lends cmyk.rules._wayAreaText
*/

dojo.declare("cmyk.rules._wayAreaText",cmyk.rules.renderingInstruction,{
	/** 
	      @constructs
	      @class This is the superclass of every text rendering instruction
	      @memberOf cmyk.rules
	      @extends cmyk.rules.renderingInstruction
	      @private
	*/
	constructor: function() {
	},
});

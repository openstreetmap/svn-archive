OsmMapCallValidator is a little utility to help test if two implementations of the map call are equivalent.
It generates random bbox map calls and checks that the results of the two implementations are identical.


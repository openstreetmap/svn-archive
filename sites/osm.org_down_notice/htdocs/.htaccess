<IfModule mod_expires.c>
	ExpiresActive On
	ExpiresDefault "access plus 10 minutes"
</IfModule>

<IfModule mod_rewrite.c>
	RewriteEngine on
	RewriteRule ^([0-9]+)/([0-9]+)/([0-9]+).png /tile/tile-notice.png
</IfModule>

#<Location /api/>
#503 Service Unavailable
#Fix ME
#</Location>
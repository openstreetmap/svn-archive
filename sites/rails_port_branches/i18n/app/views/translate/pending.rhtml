<h2><%= h(@title) %></h2>

pending strings | <%= link_to "completed strings", :action => 'complete', :locale => @user.locale -%> | <%= link_to "statistics", :action => 'stats', :sort => 'percentage' -%> 

<% unless params[:locale] == "en" %>
  <br />
  <h3><%= Locale.language -%><%= " ("+Locale.country.english_name+")" unless Locale.country.nil? %>'s pending translations:</h3>
  <hr />

  <table style="width : 100%">
  <%= render :partial => 'translation', :collection => @entries %>
  </table>

  <hr />
  <%= link_to "Older Strings".t, { :page => @entry_pages.current.next } if @entry_pages.current.next %>
  <% if @entry_pages.current.next and @entry_pages.current.previous %>
  |
  <% end %>
  <%= link_to "Newer Strings".t, { :page => @entry_pages.current.previous } if @entry_pages.current.previous %>

<% else %>

  <hr />
  <p>English is the default language for this website. So no translation required in this locale. To see translation update for other languages, <%= link_to "select the prefered one in your settings", :controller => 'user', :action => 'account', :display_name => @user.display_name %>. Also check l10n status in <%= link_to "statistics", :action => 'stat' -%>.</p>

<% end %>

<br />

<p><%= rss_link_to :action => 'rss', :locale => params[:locale], :id => 'pending' %></p>
<%= auto_discovery_link_tag :atom, :action => 'pending_rss' %>

<% Locale.set  @user.locale %>

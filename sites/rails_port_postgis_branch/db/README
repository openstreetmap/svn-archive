OSM-on-Rails-on-PostGIS
-----------------------

last updated 30 Apr 2007

rebuild.sh      -- shell script to rebuild the database from scratch
    * edit to point to lwpostgis.sql etc. on your system
    * run as "sh rebuild.sh openstreetmap"

import_osm.py   -- import script.
    * run as "python import_osm.py openstreetmap <planet.osm"

osm_server.sql  -- server tables

spatial.sql     -- spatial columns, indexes, functions, triggers

